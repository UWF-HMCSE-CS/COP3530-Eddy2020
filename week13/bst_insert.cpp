
BSTInsert(int key)
{
	TreeNode* newNode = new TreeNode(key);

	if(root == nullptr)
	{
		root = newNode;
		return;
	}
	
	if(root->GetLeftChild() == nullptr && root->GetRightChild == nullptr)
	{
		
	}
}



